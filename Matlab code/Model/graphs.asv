function graphs(i, j, t, values)
    figure();
    force = [5, 1, 0.2];
    L = [1, 0.]
    if i == 1
        mesg = sprintf('Force = %.1f N', force(j));
        subplot(1,2,1);plot(t, values(:, 1));legend(mesg);
        subplot(1,2,2);plot(t, values(:, 3));
        sgtitle('Force changes')
    elseif i == 2
    else
    end
end